import ProfileContainer from "@/app/containers/ProfileContainer/ProfileContainer";

export const metadata = { title: "My Profile | artPetShop" };

export default function ProfilePage() {
  return <ProfileContainer />;
}
