/**
 * ─── Icon System ─────────────────────────────────────────────────────────────
 * All icons from @phosphor-icons/react (v2.1.x non-deprecated names).
 * Import icons ONLY from this file — future library swap = 1-file change.
 *
 * Weight guide:
 *   duotone — hero / empty-state / decorative (premium feel)
 *   bold    — button icons (visible, accessible)
 *   regular — inline / meta icons
 *   fill    — active / selected states
 */

export {
  // ── Shopping & Commerce ──────────────────────────────────────────────────
  ShoppingCartIcon      as IconCart,
  ShoppingBagIcon       as IconBag,
  ShoppingCartSimpleIcon as IconCartSimple,
  TagIcon               as IconTag,
  CreditCardIcon        as IconCard,
  MoneyIcon             as IconMoney,
  WalletIcon            as IconWallet,
  ReceiptIcon           as IconReceipt,
  ReceiptXIcon          as IconReceiptX,
  PackageIcon           as IconPackage,
  GiftIcon              as IconGift,
  PercentIcon           as IconPercent,

  // ── Navigation ───────────────────────────────────────────────────────────
  HouseIcon             as IconHome,
  MagnifyingGlassIcon   as IconSearch,
  MagnifyingGlassMinusIcon as IconSearchOff,
  BellIcon              as IconBell,
  BellRingingIcon       as IconBellActive,
  BellSlashIcon         as IconBellOff,
  ArrowRightIcon        as IconArrowRight,
  ArrowLeftIcon         as IconArrowLeft,
  ArrowSquareOutIcon    as IconExternalLink,
  CaretRightIcon        as IconChevronRight,
  CaretLeftIcon         as IconChevronLeft,
  CaretDownIcon         as IconChevronDown,
  CaretUpIcon           as IconChevronUp,
  DotsThreeIcon         as IconMoreHoriz,
  DotsThreeVerticalIcon as IconMoreVert,
  ListIcon              as IconList,
  SquaresFourIcon       as IconGrid,

  // ── User & Account ───────────────────────────────────────────────────────
  UserIcon              as IconUser,
  UserGearIcon          as IconUserSettings,
  UsersIcon             as IconUsers,
  SignOutIcon           as IconLogout,
  LockIcon              as IconLock,
  LockOpenIcon          as IconLockOpen,
  ShieldCheckIcon       as IconShield,
  SealCheckIcon         as IconVerified,
  EyeIcon               as IconEye,
  EyeSlashIcon          as IconEyeOff,
  KeyIcon               as IconKey,

  // ── Actions ──────────────────────────────────────────────────────────────
  PlusIcon              as IconAdd,
  MinusIcon             as IconRemove,
  XIcon                 as IconClose,
  CheckIcon             as IconCheck,
  CheckCircleIcon       as IconCheckCircle,
  XCircleIcon           as IconCancel,
  TrashIcon             as IconDelete,
  PencilSimpleIcon      as IconEdit,
  CopyIcon              as IconCopy,
  DownloadSimpleIcon    as IconDownload,
  UploadSimpleIcon      as IconUpload,
  ShareIcon             as IconShare,
  BookmarkSimpleIcon    as IconBookmark,
  HeartIcon             as IconHeart,
  StarIcon              as IconStar,
  StarHalfIcon          as IconStarHalf,
  ThumbsUpIcon          as IconThumbUp,
  FlagIcon              as IconFlag,

  // ── Status & Feedback ────────────────────────────────────────────────────
  WarningIcon           as IconWarning,
  InfoIcon              as IconInfo,
  QuestionIcon          as IconHelp,
  CheckSquareIcon       as IconDoneAll,
  HourglassMediumIcon   as IconPending,
  ArrowsClockwiseIcon   as IconRefresh,
  ArrowCounterClockwiseIcon as IconReorder,
  SpinnerIcon           as IconSpinner,

  // ── Delivery & Orders ────────────────────────────────────────────────────
  TruckIcon             as IconShipping,
  MapPinIcon            as IconLocation,
  MapPinIcon            as IconHomePin,
  NavigationArrowIcon   as IconNavigate,
  AirplaneIcon          as IconAirplane,

  // ── Services & Pets ──────────────────────────────────────────────────────
  PawPrintIcon          as IconPaw,
  BoneIcon              as IconBone,
  ScissorsIcon          as IconGroom,
  StethoscopeIcon       as IconVet,
  BrainIcon             as IconTraining,
  PillIcon              as IconPill,
  SyringeIcon           as IconSyringe,
  FishIcon              as IconFish,
  BirdIcon              as IconBird,
  BowlFoodIcon          as IconBowlFood,
  ForkKnifeIcon         as IconForkKnife,
  GameControllerIcon    as IconGameController,
  BedIcon               as IconBed,
  ArmchairIcon          as IconArmchair,
  WavesIcon             as IconWaves,
  DropIcon              as IconDrop,
  FlowerIcon            as IconFlower,
  CookieIcon            as IconCookie,
  CalendarIcon          as IconCalendar,
  CalendarCheckIcon     as IconCalendarCheck,
  CalendarPlusIcon      as IconCalendarAdd,
  ClockIcon             as IconClock,
  ClockCounterClockwiseIcon as IconHistory,
  FirstAidIcon          as IconMedical,
  HeartbeatIcon         as IconHealth,
  LeafIcon              as IconEco,

  // ── Communication ────────────────────────────────────────────────────────
  EnvelopeIcon          as IconMail,
  PhoneIcon             as IconPhone,
  ChatCircleIcon        as IconChat,
  HeadsetIcon           as IconSupport,
  PaperPlaneTiltIcon    as IconSend,

  // ── Media & Social ───────────────────────────────────────────────────────
  CameraIcon            as IconCamera,
  GlobeIcon             as IconGlobe,
  YoutubeLogoIcon       as IconYoutube,
  InstagramLogoIcon     as IconInstagram,
  ImageSquareIcon       as IconImage,
  ImagesIcon            as IconImages,
  VideoCameraIcon       as IconVideo,

  // ── Finance ──────────────────────────────────────────────────────────────
  QrCodeIcon            as IconQR,
  BankIcon              as IconNetbanking,

  // ── Filters & Controls ───────────────────────────────────────────────────
  FunnelIcon            as IconFilter,
  ArrowsDownUpIcon      as IconSort,
  SlidersHorizontalIcon as IconSliders,

  // ── Misc ─────────────────────────────────────────────────────────────────
  SunIcon               as IconSun,
  MoonIcon              as IconMoon,
  InfinityIcon          as IconInfinity,
  SparkleIcon           as IconSparkle,
  LightningIcon         as IconLightning,
  ChartBarIcon          as IconChart,
  ListChecksIcon        as IconChecklist,
  FolderUserIcon        as IconFolderUser,
  HexagonIcon           as IconHexagon,
} from "@phosphor-icons/react";
